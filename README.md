# in-carbonapi
執行須在終端機輸入指令:

    ./gredlew build

    ./gredlew bootrun
    
之前的配置:

    -src
        -main
            -resources
                -libs
                    -invoice_1114-1.0-SNAPSHOT.jar
                
                -labels
                    -labels.txt
                
                -MLmodels
                    -NB.model
